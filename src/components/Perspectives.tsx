import { Container, Space, Text, Title } from '@mantine/core';

type Props = {
}

const Perspectives = (props: Props) => {
  return (
    <Container
      style={{ marginLeft: 30, marginTop: 86 }}
    >
      <Title order={3}>Perspectives </Title>
    </Container>
  )
}

export default Perspectives